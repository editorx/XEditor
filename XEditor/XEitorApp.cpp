#include <stdio.h>
#include "common.h"

int WINAPI WinMain(HINSTANCE hInst, HINSTANCE hPrevInstance,
	LPSTR lpCmd, int nShow)
{
	XEditor xEditor(hInst);

//#ifdef _DEBUG
//	DEBUG::RedirectIOToConsole();
//#endif
	TCHAR szProgram[40];
	//LoadString(hInst, IDC_APP_NAME, szProgram, 40);

}